services:
  - type: web
    name: cinego-premium
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.16.0
      - key: PORT
        value: 10000
      - key: SESSION_SECRET
        generateValue: true
